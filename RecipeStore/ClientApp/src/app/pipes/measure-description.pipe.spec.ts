import { MeasureDescriptionPipe } from './measure-description.pipe';

describe('MeasureDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new MeasureDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
